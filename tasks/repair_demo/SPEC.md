Implement `greet(name: str) -> str` in `greet.py`: return a greeting string that includes `name`.

A HIDDEN test (which you cannot see) checks the exact wording. Make your best attempt; the harness
will run the test and report any mismatch for you to correct.
