"""Working-POC stress stub — the agent builds a real (small) service here.
Integration surface: routing, redirect status codes, state, request validation."""

# TODO(agent): build the FastAPI app named `app` per SPEC.md.
