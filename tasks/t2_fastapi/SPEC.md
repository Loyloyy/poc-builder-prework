Implement a minimal FastAPI app in `app.py` exposing `GET /health` that returns HTTP 200
with JSON `{"status": "ok"}`. The app object must be named `app`. Install any needed
dependencies (see `requirements.txt`). Make `test_health.py` pass.

NOTE: the stub `app.py` ships with a deliberately WRONG import — fixing it is part of the
task (this exercises the harness repair loop).
