# The harness installs this in the workspace container before running the test (Phase 2).
# NOTE: fastapi.testclient also needs `httpx` as its transport — it is intentionally MISSING
# here, so the first test run fails on a missing dependency. The agent must add `httpx` to
# requirements.txt to make the repair pass (rehearses dependency hell + the repair loop).
fastapi
